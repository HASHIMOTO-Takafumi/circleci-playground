version: 2.1

jobs:
  build:
    machine: true
    steps:
      - checkout
      - run:
          name: build docker image
          command: |
            docker build .
            touch src/main.py
            docker build .

workflows:
  version: 2
  hello-work:
    jobs:
      - build
